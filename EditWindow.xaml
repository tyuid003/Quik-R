<Window x:Class="QuickReply.EditWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Width="420"
        Height="360"
        WindowStartupLocation="CenterScreen"
        ResizeMode="NoResize"
        Title="Edit Quick Reply"
        ShowInTaskbar="False"
        Topmost="True">
    <Grid Margin="14">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto" />
            <RowDefinition Height="*" />
            <RowDefinition Height="Auto" />
            <RowDefinition Height="Auto" />
        </Grid.RowDefinitions>

        <StackPanel>
            <TextBlock Text="Balloon Name (Optional)" FontWeight="SemiBold" />
            <TextBox x:Name="DisplayNameTextBox"
                     Margin="0,6,0,0"
                     FontSize="13" />
        </StackPanel>

        <TextBox x:Name="MessageTextBox"
                 Grid.Row="1"
                 Margin="0,12,0,0"
                 AcceptsReturn="True"
                 TextWrapping="Wrap"
                 VerticalScrollBarVisibility="Auto"
                 FontSize="14" />

        <Grid Grid.Row="2" Margin="0,12,0,0">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="*" />
                <ColumnDefinition Width="Auto" />
            </Grid.ColumnDefinitions>

            <StackPanel>
                <TextBlock Text="Color" FontWeight="SemiBold" />
                <StackPanel Orientation="Horizontal" Margin="0,6,0,0">
                    <Button Width="110" Height="28" Content="Choose Color..." Click="PickColorButton_OnClick" />
                    <Border x:Name="ColorPreview" Width="36" Height="28" Margin="10,0,0,0" CornerRadius="6" BorderThickness="1" BorderBrush="#808080" />
                    <TextBlock x:Name="ColorHexText" Margin="10,0,0,0" VerticalAlignment="Center" />
                </StackPanel>
            </StackPanel>

            <StackPanel Grid.Column="1" Margin="14,0,0,0" Width="130">
                <TextBlock Text="Opacity" FontWeight="SemiBold" />
                <Slider x:Name="OpacitySlider"
                        Margin="0,6,0,0"
                        Minimum="0.25"
                        Maximum="1"
                        TickFrequency="0.05"
                        IsSnapToTickEnabled="True" />
                <TextBlock x:Name="OpacityValueText" HorizontalAlignment="Right" />
            </StackPanel>
        </Grid>

        <StackPanel Grid.Row="3"
                    Orientation="Horizontal"
                    HorizontalAlignment="Right"
                    Margin="0,12,0,0">
            <Button Width="80" Margin="0,0,8,0" Content="Cancel" IsCancel="True" />
            <Button Width="80" Content="Save" IsDefault="True" Click="SaveButton_OnClick" />
        </StackPanel>
    </Grid>
</Window>
